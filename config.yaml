# Meridian 全局配置
server:
  host: 0.0.0.0
  port: 6100

# K线数据根目录
data_dir: F:\VCPToolBox\wyckoff\data

# 插件开关（未列出的插件默认启用）
plugins:
  datasource:
    enabled: true
  annotation:
    enabled: true
  # engine:
  #   enabled: false
  # trading:
  #   enabled: false
