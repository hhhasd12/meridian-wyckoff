# V4 状态机后续路线图 — 从"架构完成"到"可用"

> 状态：待用户审核
> 前置：认知架构重构(cognitive-architecture-refactor) 全部完成
> 日期：2026-03-23

## 当前已完成

| 阶段 | 内容 | 测试 |
|------|------|------|
| V4核心 | 三大原则打分器 + 序列化推进器 + 22检测器 | 29 |
| V4推进器 | state_machine_v4.py + boundary_manager + detector_registry | 30→49 |
| Phase C | 5个瓶颈修复 + 渲染加速 + StructureContext四字段 | — |
| 识别层修复 | UPTREND/DOWNTREND评分 + 结构超时重置 + IDLE出口 | — |
| **认知架构** | MarketMode三模式 + BoundaryManager接入 + StructureHypothesis + VOL约束 + 冷启动 | 95 |
| API+前端 | V4数据暴露 + PrinciplesPanel + AnalysisPage | — |

**总计**: 236广域测试通过, 0 failed

## 当前未完成的问题

### P0 — 不解决就不能用

1. **检测器节奏不对**
   - 诊断: 2000根H4, 结构平均30-36根(5-6天)就跑完，B阶段6-13根就被跳过
   - 原因: 没有最小阶段停留时间，每个事件3-5根确认就推进
   - 认知架构重构**部分缓解**(MarketMode门控)，但**未验证修复效果**
   - 需要: 用真实数据重新诊断，确认改进幅度

2. **未用真实数据验证过**
   - V4所有调优都是用合成数据/单元测试
   - 真实BTC/USDT H4数据上的表现完全未知
   - 需要: 拉取真实数据 → 跑状态机 → 看输出是否合理

### P1 — 短期内要做

3. **进化系统参数失效**
   - V4行为和V2完全不同，之前进化出的最优参数对V4无效
   - 需要重新校准进化参数空间(StateConfig中的阈值)
   - 依赖P0完成(先让检测器正确，再让进化去优化)

4. **前端AnalysisPage布局bug**
   - 上方空白、K线图位置异常
   - PrinciplesPanel 已接入但AnalysisPage还有渲染问题

5. **plugin.py 死壳**
   - wyckoff_state_machine/plugin.py 是空壳，engine.py直接调V4
   - 不影响运行但接口不完整，冒烟测试可能查到

### P2 — 功能增强(非必须)

6. Creek/Ice线检测 — SOS/SOW的突破标的
7. Spring三分类 — 1号(高量)/2号(低量理想)/3号(未跌破)
8. 区间斜率 — higher lows = 更强的吸筹
9. 多时间框架联动 — H4结构 + H1确认 + M15入场

---

## 建议执行路线

### Wave 1: 真实数据验证 (1天)

**目标**: 用真实BTC H4数据看状态机输出，建立改进基线

| # | 任务 | 说明 |
|---|------|------|
| 1 | 拉取数据 | `fetch_data.py` 获取BTC/USDT H4最近2000根 |
| 2 | 诊断脚本 | 写 `scripts/v4_diagnosis.py`，逐根喂K线，输出: 转换时间线、每阶段持续bar数、结构数量、IDLE比例 |
| 3 | 对比分析 | 对比认知架构重构前后的数据(之前诊断: 153次变化/21循环/30-36根/循环) |
| 4 | 决策 | 如果节奏合理 → 进Wave 2; 如果仍太快 → 加B阶段最小停留时间 |

### Wave 2: 检测器校准 (2-3天, 视Wave 1结果)

| # | 任务 | 说明 |
|---|------|------|
| 5 | B阶段最小时间 | 如果诊断结果仍然太快，给B阶段加 `min_phase_bars=20` |
| 6 | 检测器阈值 | 基于真实数据中的误检/漏检，逐个调整检测器参数 |
| 7 | 前端验证 | 启动系统，在AnalysisPage上看V4标注是否合理 |

### Wave 3: 进化重校准 (1-2天)

| # | 任务 | 说明 |
|---|------|------|
| 8 | 参数空间更新 | 更新 config.yaml 的进化参数范围(适配V4行为) |
| 9 | 跑一轮进化 | GA+WFA验证，确认V4下能产生正Sharpe |
| 10 | 结果对比 | V4进化结果 vs 之前V2进化结果 |

### Wave 4: 收尾 (1天)

| # | 任务 | 说明 |
|---|------|------|
| 11 | plugin.py修复 | 补全死壳接口 |
| 12 | AnalysisPage布局 | 修复前端bug |
| 13 | 代码清理+commit | 整理所有未提交改动 |

---

## 决策点

- **Wave 1 完成后**: 看诊断结果决定Wave 2的工作量
- **Wave 3 完成后**: 决定是否接真实交易所(Paper Trading)
- **P2 功能**: 等系统稳定运行后再考虑

